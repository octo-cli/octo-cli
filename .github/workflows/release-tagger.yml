name: release-tagger
on:
  push:
    branches:
      - master
  pull_request:
    types:
      - labeled
jobs:
  tag-release:
    runs-on: ubuntu-18.04
    steps:
      - uses: actions/checkout@v2
      - run: script/tag-release
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
